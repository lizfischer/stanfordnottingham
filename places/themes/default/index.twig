{% extends "base.twig" %}


{% block body %}

    <h1>{{ current_page.title }}</h1>
    <img src="{{ meta.img }}" class="header-img">
    <div class="dropcap">
        {{ parent() }}
    </div>
{% endblock %}