{% extends "noSidebar.twig" %}

{% block body %}

{{ parent() }}
<h2>Creative Works</h2>
    <p>Drawings, poetry, reflections, and fiction produced during the seminar. Most were made with the prompts stone, green, fragmentary, and boundary in mind.</p>
    <hr>
    {% for page in pages|sort_by("time")|reverse %}
        {% if page.id starts with "extras/" %}
            <div class="post">
                <h3><a href="../places?{{ page.id }}">{{ page.title }}</a></h3>
                <p class="author">by {{ page.author }} - {{ page.date_formatted }}</p>
                <p class="excerpt">{{ page.description }}</p>
            </div>
        {% endif %}
    {% endfor %}

    <div class="row ">
        <div class="offset-md-2 col-md-12 gallery">
            <div class="row">
                {% for image in meta.images %}
                {% if loop.index0 is divisible by (3) %}
            </div>
            <div class="row">
                {% endif %}
                <a href="../img/extras/{{ image[0] }}" data-toggle="lightbox" data-footer="{{ image[1] }}" data-gallery="example-gallery" class="col-sm-4">
                    <img src="../img/extras/{{ image[0] }}" class="img-fluid thumb">
                </a>
                {% endfor %}
            </div>
        </div>
    </div>
    </div>
    <script>
        var cw = $('.thumb').width();
        $('.thumb').css({
            'height': cw + 'px'
        });
    </script>
    <script>
        $(document).on('click', '[data-toggle="lightbox"]', function(event) {
            event.preventDefault();
            $(this).ekkoLightbox();
        });
    </script>

{% endblock %}
